# Core dependencies
flask>=3.1.0
anthropic>=0.30.0
requests>=2.32.0
python-a2a==0.5.6
mcp>=1.0.0
python-dotenv>=1.0.0
flask-cors>=4.0.0
pymongo>=4.0.0

# X402 Payment System Dependencies
# Note: Using mock implementation - no blockchain dependencies needed
# x402-a2a should be installed from the official repo using setup_x402_dependencies.sh

# Additional utilities for X402
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
httpx>=0.25.0

# Development and debugging tools
pytest>=7.0.0
pytest-asyncio>=0.20.0
